# Clé API Google Maps (Maps JavaScript API + Places API + Geocoding API activées).
# Voir ibk-express-prompt-conception.md, section "Instructions de configuration Google Maps".
# Sans cette clé, l'app bascule sur le mode de saisie manuelle (adresse texte + ville).
VITE_GOOGLE_MAPS_API_KEY=
